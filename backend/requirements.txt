# Зависимости backend FinTrack. Используется в Dockerfile (`pip install -r requirements.txt`).
# Версии указаны минимальной планкой, чтобы при пересборке образа подтянулись свежие патчи.

# Web-фреймворк и сервер
fastapi[standard]>=0.115

# ORM и async-драйверы PostgreSQL
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
psycopg[binary]>=3.2

# Миграции
alembic>=1.13

# Конфиг через переменные окружения
pydantic-settings>=2.0

# Авторизация: хэш Argon2id для паролей (NFR-04 ВКР, рекомендация OWASP)
argon2-cffi>=23.1

# JSON Web Tokens — выпуск и проверка access-токенов
PyJWT>=2.9

# Валидация EmailStr в Pydantic (без него EmailStr выдаёт ImportError)
email-validator>=2.2

swagger-ui-bundle>=1.0.0,<2.0.0
