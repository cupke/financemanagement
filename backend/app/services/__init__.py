"""Сервисный слой FinTrack.

  Здесь живёт бизнес-логика, которая не привязана к HTTP (то есть её можно вызвать
  не только из FastAPI-эндпоинта, но и из CLI-команды, фонового джоба, теста).
  Это соответствует требованию МУ (п. 4.9.Б) о трёхзвенной архитектуре:
      REST-контроллер (app/api) → сервис (app/services) → репозиторий (ORM-запросы).
  """