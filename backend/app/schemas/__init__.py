"""Pydantic-схемы — то, как данные выглядят на границе API.

Схемы (DTO) сознательно отделены от ORM-моделей:
- ORM-модели (app.db.models) описывают структуру БД,
- Pydantic-схемы описывают форму JSON-запросов и ответов API.

Это даёт нам свободу: можно изменить поле в БД, не ломая API, и наоборот —
можно эволюционировать API без изменения схемы БД (требование МУ:
SOLID, в частности SRP — каждый класс отвечает за одно).
"""
